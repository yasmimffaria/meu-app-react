/** @type {import("prettier").Config} */
export default {
    semi: true,             // Usa ponto e virgula no final das linhas
    singleQuote: true,      // Usa aspas simples em vez de duplas
    useTabs: false,         // Usa espaços em vez de tabs
    bracketSpacing: true,   // Espaço entre chaves em objetos { foo: bar }
    arrowParens: 'always',  // Sempre usa parenteses em funções arrow
    endOfLine: 'auto',      // Mantém o estilo de fim de linha do sistema
};